<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Test</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body style="background-color: #e5e7eb">
    <div class="flex justify-center h-screen">
        <form class="space-y-6 w-full sm:w-1/2" action="/cryptoChoice" method="POST" id="choiceForm">
            <label class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Select Cryptocurrency</label>
            <select id="crypto" type="text" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500" required>
                <option value="5000">Tron</option>
                <option value="5010">Test Tron</option>
            </select>
            <div class="mx-auto w-full sm:w-auto">
                <button type="submit" class="flex w-full sm:w-auto rounded-md bg-gray-800 hover:bg-gray-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Choose</button>
            </div>
        </form>
    </div>

    <crypto-oris
            x-app-ec-from = '<%= ec_from%>'
            x-app-ec-sign-r = "<%= sign.r%>"
            x-app-ec-sign-s = "<%= sign.s%>"
            x-app-ec-sign-v = "<%= sign.v%>"
            x-app-ec-msg = "<%= sign.message%>">
    </crypto-oris>

<script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.js"></script>
<button onclick="loadCryptoOrisWidget()">Test</button>
<script src="/v1/widget/js"></script>
</body>
</html>